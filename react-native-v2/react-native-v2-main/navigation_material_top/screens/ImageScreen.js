import { View, Text } from "react-native";

const ImageScreen = (props) => {
    return (
        <View style={{ marginTop: 50 }}>
            <Text style={{ alignSelf: "center", margin: 20 }}>ImageScreen</Text>
        </View>
    );
};

export default ImageScreen;
